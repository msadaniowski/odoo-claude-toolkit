# Research — `<module>` `<from>` → `<to>`

> Filled by AI in Phase 1. This is a **read-only** discovery pass. No code changes.

## 1. Current module inventory

- **Manifest declared version**: `<x.y.z>`
- **Python files**: `<count>`, ~`<LOC>` LOC
- **Models defined / inherited**: `<list>`
- **Views**: `<count>` (form / tree / search / kanban / report)
- **Wizards**: `<list>`
- **Reports (QWeb / legacy)**: `<list>`
- **Tests present**: [ ] Yes, coverage of: `<list of what's tested>`  [ ] No  [ ] Partial
- **External dependencies (Python / system)**: `<list>`
- **Module dependencies (`depends`)**: `<list>`

## 2. Delta analysis

### API changes (core Odoo)
> What changed between `<from>` and `<to>` that this module is affected by.
-

### Deprecated / removed features used
-

### ORM / field / decorator changes relevant to this module
-

### View / XML changes (e.g. `attrs`, `states`, tree→list rename)
-

### JS / OWL framework changes (if module has static/src)
-

## 3. OpenUpgrade coverage

- **Models this module touches that OpenUpgrade already handles**: `<list>`
- **Models that are module-private**: `<list>` → these need our own pre/post scripts
- **Link to relevant OpenUpgrade scripts**: `<url>`

## 4. Risk surface

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
|      |        |            |            |

## 5. Known unknowns
> Things the AI could not determine from static analysis and need human answers before Phase 2.
-
